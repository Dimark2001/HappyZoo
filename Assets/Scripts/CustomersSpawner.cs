using UnityEngine;

public class CustomersSpawner : MonoBehaviour
{
    [SerializeField] private int _maxCutomers;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
